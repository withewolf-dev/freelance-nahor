import 'package:flutter/material.dart';

class UploadWorkPage extends StatelessWidget {
  const UploadWorkPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "upload work",
          textDirection: TextDirection.ltr,
        ),
      ),
    );
  }
}
