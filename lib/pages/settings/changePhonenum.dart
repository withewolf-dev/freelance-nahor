import 'package:flutter/material.dart';

class ChangePhonenumPage extends StatelessWidget {
  const ChangePhonenumPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "change phone",
          textDirection: TextDirection.ltr,
        ),
      ),
    );
  }
}
